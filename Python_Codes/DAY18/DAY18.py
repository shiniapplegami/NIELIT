import numpy as np

a=np.arange(6, dtype="i4").reshape(2,3)

print(a.dtype)

#-----------------------------------------------------------------------------------------------------------

ac=a.copy()

print(ac.astype("f8"))
#----------------------------------------------------------------------------------------------------------

a3=ac.astype(np.int64)

#---------------------------------------------------------------------------------------------------------

a4=ac.astype(np.str)

#---------------------------------------------------------------------------------------------------------
a4=np.array([[1,2,3],[4,5,6]])
a4=ac.astype(np.str)

#-------------------------------------------------------------------------------------------------------

a1=np.array([range(1,13,1)]).reshape(4,3)
print(a1[[0],:])
print(a1[[2],:])
print(a1[:,[1]])
print(a1[1:3,0,2])
print(a1[:,-1])

#-------------------------------------------------------------------------------------------------------

n=np.array([range(11,51,1)]).reshape(10,4)
n[n%5==0]=-1

#------------------------------------------------------------------------------------------------------

def pto(array):
    mod=[]
    for i in range(array.shape[0]):
        for j in range(array.shape[1]):
            if i==j:
                mod.append(array[i][j])
    print(mod)

#---------------------------------------------------------------------------------------------------

def occur(array,num):
    p=0
    for i in range(array.shape[0]):
        for j in range(array.shape[1]):
            if array[i][j]==num:
                p+=1
    print(p)
